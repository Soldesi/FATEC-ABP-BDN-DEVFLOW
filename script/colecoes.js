use leads_db

db.createCollection("users")
db.createCollection("teams")
db.createCollection("stores")
db.createCollection("customers")
db.createCollection("leads")
db.createCollection("negotiations")
db.createCollection("logs")
